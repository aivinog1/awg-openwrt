name: Build AmneziaWG (manual)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: openwrt/openwrt
          ref: v23.05.6
          fetch-depth: 0

      - name: Cache Tools and Kernel
        id: cache-tools-kernel
        uses: actions/cache@v4
        env:
          cache-name: "cache-tools-kernel-23.05.6-aarch64_cortex-a53-mediatek-filogic"
        with:
          path: |
            build_dir
            staging_dir
            tmp
          key: ${{ runner.os }}-build-${{ env.cache-name }}

      - name: Building kernel and tools
        run: |
          echo ">>> Setup feeds"
          wget https://downloads.openwrt.org/releases/23.05.6/targets/mediatek/filogic/feeds.buildinfo -O feeds.conf
          echo "src-git awgopenwrt https://github.com/yury-sannikov/awg-openwrt.git" >> ./feeds.conf
          ./scripts/feeds update && ./scripts/feeds install -a

          echo ">>> Setup config"
          wget https://downloads.openwrt.org/releases/23.05.6/targets/mediatek/filogic/config.buildinfo -O .config
          echo "CONFIG_PACKAGE_kmod-amneziawg=m" >> .config
          echo "CONFIG_PACKAGE_amneziawg-tools=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-amneziawg=y" >> .config
          echo "CONFIG_PACKAGE_kmod-crypto-lib-chacha20=m" >> .config
          echo "CONFIG_PACKAGE_kmod-crypto-lib-chacha20poly1305=m" >> .config
          echo "CONFIG_PACKAGE_kmod-crypto-chacha20poly1305=m" >> .config

          make defconfig

          echo ">>> make tools/install"
          make tools/install -i -j$(nproc)

          echo ">>> make toolchain/install"
          make toolchain/install -i -j$(nproc)

          echo ">>> make target/linux/compile"
          make target/linux/compile -i -j$(nproc) V=s

      - name: Build AmneziaWG
        run: |
          echo ">>> Building kmod-amneziawg"
          make package/kmod-amneziawg/{clean,download,prepare} V=s || true
          make package/kmod-amneziawg/compile V=s || true

          echo ">>> Building luci-app-amneziawg"
          make package/luci-app-amneziawg/{clean,download,prepare}
          make package/luci-app-amneziawg/compile V=s

          echo ">>> Building amneziawg-tools"
          make V=s package/amneziawg-tools/{clean,download,prepare}
          make V=s package/amneziawg-tools/compile

      - name: Prepare artifacts
        run: |
          mkdir -p awgrelease
          postfix="manual_v23.05.6_aarch64_cortex-a53_mediatek_filogic"
          cp bin/packages/aarch64_cortex-a53/awgopenwrt/amneziawg-tools_*.ipk awgrelease/amneziawg-tools_${postfix}.ipk
          cp bin/packages/aarch64_cortex-a53/awgopenwrt/luci-app-amneziawg_*.ipk awgrelease/luci-app-amneziawg_${postfix}.ipk
          cp bin/targets/mediatek/filogic/packages/kmod-amneziawg_*.ipk awgrelease/kmod-amneziawg_${postfix}.ipk

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: amneziawg-ipks
          path: awgrelease/*.ipk
